<h1><%= @wiki.title %></h1>

	<!--% if policy(Page.new).edit? %-->
	<%= link_to "Edit Wiki", edit_wiki_path, class: 'btn btn-success' %>
	<!--% end %-->

<div class="row">
  <div class="col-md-8">
	<br />
    <p class="lead"><%= @wiki.description %></p>
    <%= render @pages %>
    <!--%= will_paginate @posts %-->
  </div>
  <br />
   <div class="col-md-4">
     <!--% if policy(Page.new).create? %-->
       <!--%= link_to "New Page", new_wiki_page_path(@page), class: 'btn btn-success' %-->
     <!--% end %-->

     <!--% if policy(@topic).destroy? %-->
       <!--%= link_to "Delete Wiki", @wiki, method: :delete, class: 'btn btn-danger', confirm: 'Are you sure you want to delete this wiki?' %-->
     <!--% end %-->
   </div>
 </div>
