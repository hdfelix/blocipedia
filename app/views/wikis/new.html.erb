<h1>Create a Wiki</h1>

<%= form_for @wiki do |f| %>
	<%= f.label :title %>
	<%= f.text_field :title, autofocus: true %>

	<%= f.label :description %>
	<%= f.text_field :description %>

	<%= f.submit %>
<% end %>
